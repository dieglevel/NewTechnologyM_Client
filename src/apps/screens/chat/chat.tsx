import { SafeAreaView } from "@/apps/components";
import { ChatHeader } from "@/apps/components/chat";

export const Chat = () => {
      return (
         <SafeAreaView>
            <ChatHeader />
         </SafeAreaView>
      );
   }